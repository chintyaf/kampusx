import React from 'react'

const SectionSidebar = () => {
  return (
    <>

    </>
  )
}

export default SectionSidebar

  // selectedRow === null ? (
  //         <SessionSidebar days={days} />
  //       ) : (
  //         <SessionForm key={selectedRow.id} data={selectedRow} />
  //       )